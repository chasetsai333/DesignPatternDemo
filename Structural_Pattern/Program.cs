using System;

namespace Structural_Pattern
{
    class Program
    {
        static void Main(string[] args)
        {
            //new Adapter_Pattern.Adapter().Go();

            //new Bridge_Pattern.Bridge().Go();

            //new Composite_Pattern.Composite().Go();

            //new Decorator_Pattern.Decorator().Go();

            //new Facade_Pattern.Facade().Go();

            //new Flyweight_Pattern.Flyweight().Go();

            new Proxy_Pattern.Proxy().Go();
        }
    }
}
